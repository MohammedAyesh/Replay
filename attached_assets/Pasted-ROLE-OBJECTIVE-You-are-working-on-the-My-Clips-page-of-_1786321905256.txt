ROLE & OBJECTIVE

You are working on the "My Clips" page of Replay. This is a purely 
frontend UI/UX task — two small, low-risk visual changes. No backend 
logic, timing calculations, or data handling should change. The 
export pipeline itself (task #103) is already complete and correct; 
do not modify anything in userClips.ts, ffmpegExport.ts, or bunny.ts.

Before touching any code: review the current My Clips page (my-clips.tsx 
and any related clip-card/player components) and state your proposed 
changes — including the exact files you'll touch — before implementing 
anything.

CHANGE 1 — Higher-quality download note

Add a small, unobtrusive note or badge inside the clip card, near or 
below the video player, that tells the user downloading gives them a 
better-quality file than in-browser playback. Use wording like 
"💡 Tip: Download for the best quality" — avoid the word "full," which 
overstates it slightly; "best" or "high" quality is accurate, "full" 
implies something stronger than what's actually delivered. Keep it 
visually quiet — a small caption or subtle badge that fits the existing 
dark-mode sports-tech design system, not a banner or anything that 
competes with the video player or the export/download button for 
attention.

CHANGE 2 — Step-based export loading state

Replace the current technical stage labels ("Fetching…", "Encoding…", 
"Uploading…") shown on the Export button while polling with a simple 
step counter instead — something like "Step 1/3", "Step 2/3", 
"Step 3/3" — accompanied by a smooth spinner animation. The backend 
already tracks exactly three stages via the existing exportProgress 
map (fetching, encoding, uploading, in that fixed order) — map those 
directly to steps 1, 2, and 3. This is a display-only change: leave 
the backend's stage tracking completely untouched, you're only changing 
what's rendered to the user, not the underlying data.

A short, plain-language subtitle under the step count is fine if it 
reads cleanly (e.g. "Step 2/3 — Preparing your video") but is not 
required — use your judgment on whether it looks clean or cluttered. 
Do not expose the raw technical stage names (fetching/encoding/uploading) 
in the visible label regardless of which version you choose.

Since step 2 (encoding) will typically take noticeably longer than 
steps 1 and 3, make sure the spinner animation stays clearly active 
through that stage so it reads as "still working" rather than "stuck" — 
no other special handling is needed for this, a continuously animated 
spinner is sufficient.

VERIFICATION

After implementing both changes, open the My Clips page in preview and 
confirm:
- The quality note renders cleanly, fits the dark theme, and doesn't 
  clutter the card at both desktop and a narrower/mobile width.
- The export button correctly shows Step 1/3, then Step 2/3, then 
  Step 3/3 in order during a real export, with the spinner animating 
  throughout, and transitions cleanly to the Download state when done.

Report back with the actual diff and what you observed in preview — not 
a paraphrased summary — before marking this complete.
SCOPE: exactly THREE files. Two get a small additive change; one is new.
  1. artifacts/soccerwatch/src/pages/view.tsx        (CREATE — new file)
  2. artifacts/soccerwatch/src/pages/fields.tsx      (add one optional prop)
  3. artifacts/soccerwatch/src/pages/academies.tsx   (add one optional prop)

Do NOT modify App.tsx, layout.tsx, index.css, or any other file in this task.
The route and the navigation are deliberately NOT part of this turn.

GOAL: build a new container page that shows the existing Fields and Academies
pages as two sections behind a toggle. Nothing is moved, merged or deleted.
/fields and /academies must keep working exactly as they do today.

--- ATTACHMENT ---

The screenshot is a STYLE reference for the Browse title and the pill toggle
ONLY. Ignore its navigation bar (four tabs; the real app has five and the nav
is out of scope this turn). Ignore all its content and its venue cards — the
cards on both pages are already final and must not be restyled.

=========================================================
PART 1 — add an "embedded" prop to both existing pages
=========================================================

In fields.tsx and academies.tsx, change each default-exported component to
accept an optional prop:

    { embedded = false }: { embedded?: boolean }

When embedded is true, that page renders WITHOUT its own <h1> title and
subtitle block. The search bar, the card list, loading states, empty states and
all behaviour stay exactly the same.

When embedded is false or omitted, the page renders EXACTLY as it does today,
byte-for-byte in behaviour. This is critical: /fields and /academies must be
completely unaffected.

Change nothing else in these two files. Do not touch the cards, the search, the
accordion, expandedId, LiveRow, useGetAcademyRecordings, VideoPlayer, follow
logic, or any query.

=========================================================
PART 2 — create view.tsx
=========================================================

A new page component. Structure, top to bottom:

  1. A sticky header block containing:
     - An <h1> reading "Browse" — use an existing translation string if one
       fits. If no suitable string exists, use the plain English word "Browse"
       and do NOT add a new translation key.
     - Below it, a segmented pill toggle: one container, border-radius 99px,
       card background, 1px border, holding two equal-width buttons labelled
       with the EXISTING Academies and Fields title strings from the
       translations. Active button: solid primary fill with primary-foreground
       (dark) text, border-radius 99px. Inactive: transparent, muted-foreground.
     - Default selected section: Academies.

  2. Below that, render <Academies embedded /> or <Fields embedded />
     depending on the toggle. Use plain conditional rendering. Do not copy any
     code out of those files — import and render the components as they are.

Hold the selection in simple local state (useState). Do not add a route
parameter, a URL query string, or any global state.

--- LAYOUT ---

view.tsx must not break the scrolling of the page it renders. The embedded
pages already manage their own scroll container with
"flex-1 min-h-0 overflow-y-auto". view.tsx must give them a working flex
context: its outer wrapper needs "flex flex-col flex-1 min-h-0 overflow-hidden"
and the embedded child must be able to fill the remaining height.

Verify BOTH sections scroll to the bottom and the last card clears the floating
nav before you finish. This exact mistake has broken pages in this project
twice.

--- RULES ---

Use existing theme tokens. Do not hardcode hex colours. Use logical properties
for RTL (start/end, ms/me, ps/pe); never pair a logical inset with a physical
transform. Do not add any new translation key.

--- CONSTRAINTS ---

Do not modify any API call, database schema, query, or route. Do not add,
rename, or drop any database column. Do not install any package. Do not delete
any file. Do not register the new page in App.tsx — that is the next task. Do
not change the navigation.

If any change would require editing a fourth file, STOP and tell me instead.

Show me the complete diff of all three files and nothing else. Do not start any
follow-up task.